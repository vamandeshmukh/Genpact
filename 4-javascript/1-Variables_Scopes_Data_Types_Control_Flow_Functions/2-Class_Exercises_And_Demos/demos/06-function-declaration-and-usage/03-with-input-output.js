/**
 * A function that accepts two arguments (expected to be numbers/strings) and returns their sum/concatenation
 */
function sumOrConcatenate( a, b ) {
    return a + b;
}

console.log( sumOrConcatenate( 'Mr.', 'Bond' ) );	// outputs Mr. Bond
console.log( sumOrConcatenate( 1, 2 ) );	// outputs 3